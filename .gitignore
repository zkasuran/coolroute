# secrets and local env
.env
*.env
!.env.example

# python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/
.venv/
venv/

# local scratch (real captured runs are committed selectively)
artifacts/*.local

# demo-video pipeline artifacts (kept in the lane, never in the code repo)
CODEX-VIDEO-BRIEF.md
coolroute-draft.json
finish-video.sh
/DEMO-*.mp4
/upload-*/

# submission tooling (private: names, emails, form field ids, key-paste steps)
/submit/

# deploy cache
/.wrangler/
